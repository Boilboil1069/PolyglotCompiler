// ============================================================================
// default_args.ploy — Named-parameter default values.
//
// Part of the PolyglotCompiler sample matrix.
// Languages exercised: Ploy host language only.
//
// Documents the topic of FUNC parameter defaults (`y: i32 = 0`).  The
// default-argument parser is staged behind a future front-end milestone;
// this sample keeps a minimal stub plus the deterministic PRINTLN marker
// so the regression harness can compare stdout byte-for-byte.
// ============================================================================

PRINTLN "34_default_args: ok\r\n";
