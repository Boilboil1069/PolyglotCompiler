# Hermetic .pyi stub for a fake "numlib" package.  Consumed by polyc through
# --python-stubs=<this dir's parent>.  Mirrors the typeshed layout: the file's
# basename (without .pyi) becomes the importable module name.

def array(values: list) -> list: ...
def mean(values: list) -> float: ...
def sum(values: list) -> float: ...

PI: float
E: float

class Vector:
    def __init__(self, x: float, y: float) -> None: ...
    def length(self) -> float: ...
