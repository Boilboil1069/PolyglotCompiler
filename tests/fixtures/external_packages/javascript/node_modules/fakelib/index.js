// fakelib — minimal CommonJS module used by JsImportResolver tests.
function greet(name) {
    return "hello " + name;
}

function sum(a, b) {
    return a + b;
}

module.exports = { greet: greet, sum: sum };
